# Ensure consistent dependency resolution
save-exact=true

# Use npm's legacy peer deps behavior for better compatibility
legacy-peer-deps=true

# Include optional dependencies (needed for dmg-license on macOS)
include=optional

# Use npm's default registry
registry=https://registry.npmjs.org/

# Cache settings for better performance
cache=.npm-cache 